# phpbox 项目总纲

## 1. 项目定位

- **项目类型**：纯 Bash 实现的本地 Docker LNMP 多版本开发环境管理器。
- **核心服务**：PHP、MySQL、Redis、Nginx，以及站点和 hosts 管理。
- **运行边界**：只面向本地开发，不作为生产部署系统。
- **首要目标**：可查阅、可恢复、可重复安装、长操作有反馈、失败不破坏旧状态。
- **技术栈**：Bash 4.4+、Docker、Docker Compose、Alpine APK、PECL、Shell 脚本测试。

### 1.1 非目标

- 只支持本地开发，不承诺生产部署、高可用或灾难恢复能力。
- 不自动修改用户项目源码，不自动删除未明确声明的数据、配置、镜像或备份。
- 不为与当前任务无关的技术债务进行重构。
- 不把运行期生成物当作源码事实来源。

### 1.2 资源所有权

| 资源 | 所有者 | 默认处理方式 |
| --- | --- | --- |
| Bash 源码、模板、测试 | Git 仓库 | 可修改，必须经过验证 |
| `.env`、服务配置和扩展状态 | 用户/项目运行期 | 可覆盖，变更前保留旧值 |
| Compose、Dockerfile、vhost 生成物 | 生成脚本 | 修改生成逻辑后重新生成 |
| 数据目录和 Docker 数据卷 | 用户数据 | 默认保留，禁止隐式删除 |
| `offline/` 缓存 | 构建事务 | 仅成功验证后晋升 |
| 临时文件、容器、镜像和锁 | 当前事务 | 成功或失败后清理 |

## 2. 总体优化路线

以下顺序是项目长期维护的总路线，新增功能应优先复用这些边界，不另造平行机制：

1. **构建事务化**：APK 和 PECL 依赖先进入构建暂存区，Docker 镜像及扩展验证成功后再晋升到 `offline/` 正式缓存。
2. **失败可恢复**：构建、配置生成、站点变更和端口变更失败时，清理本次新增的半成品，保留旧配置、旧镜像和旧服务状态。
3. **配置单一来源**：默认配置只维护一份；用户 `.env` 负责覆盖；每个 PHP 版本保存自己的实际扩展状态。
4. **状态归位**：当前顶层 `state/` 仅保存 PHP 扩展状态，后续应迁移到 `config/php/<版本>/extensions.env`，并保留旧路径兼容迁移，确认成功后再清理旧目录。
5. **运行反馈**：每个用户可感知的阶段都输出开始、进行中、成功或失败、清理或回滚结果。
6. **可验证交付**：每次改动先做窄验证，再根据风险扩大到 Bash 语法、结构检查、行为回归和 Compose 验证。
7. **文档同步**：命令、环境变量、生成文件、备份内容和目录职责变化必须同步更新 README 和相关模板。

### 2.1 PHP 依赖构建的标准顺序

1. 校验 PHP 版本、扩展列表、镜像源和网络超时。
2. 创建本次构建独立的暂存目录。
3. PECL 包优先从 `offline/php/<版本>/pecl/` 命中，否则下载到暂存目录。
4. APK 依赖优先从 `offline/php/<版本>/apk/` 命中，否则预取完整依赖闭包到暂存目录。
5. 校验 APK 闭包和关键构建依赖的完整性。
6. 生成 Dockerfile 并执行 Docker build。
7. 在镜像内验证扩展加载、`php -m`、关键 `php --ri` 信息和 `php-fpm -t`。
8. 仅在全部验证成功后，将 APK 和 PECL 暂存内容原子晋升到 `offline/`。
9. 清理构建暂存目录，再生成 Compose 配置、启动服务并执行健康检查。
10. 任一步失败时报告阶段和退出状态，清理本次产物，保留旧离线缓存和旧服务。

### 2.1.1 APK/PECL 离线缓存提交契约

- `config/php/<版本>/{apk,pecl}/` 只是本次构建上下文的暂存区，不是离线库；暂存日志必须写出路径、包数量和“构建成功后晋升”。
- `OFFLINE_DIR` 必须在公共环境加载层统一归一化：支持默认项目路径、项目相对路径、绝对路径和 `~/...`；所有版本只能写入归一化后的 `OFFLINE_DIR/php/<版本>/{apk,pecl}`，禁止在业务模块自行拼接或创建平行目录。
- 只有同一版本镜像构建、扩展编译、扩展启用全部成功后，才能进入提交阶段；Docker 构建失败必须清理本次暂存，不能修改 `offline/` 正式库。
- APK 提交必须执行“临时目录组装 -> 包数量/关键工具链完整性校验 -> 原子替换 `offline/php/<版本>/apk/` -> 对替换后的目标再次校验”。
- 提交前保留旧正式库；目标替换或替换后校验失败时，删除不完整的新目录并恢复旧库。离线模式没有可提交 `.apk` 时必须失败，禁止静默跳过。
- 在线降级构建可以跳过 APK 晋升，但必须输出明确的跳过原因；不能把在线构建误报为已建立离线闭包。PECL 只提交本次实际下载且经过构建验证的包。
- 提交成功必须输出正式目标路径、包数量和大小；提交失败必须输出失败阶段、暂存路径、正式库是否保留以及清理结果。
- 提交结束后才清理 `config/php/<版本>/{apk,pecl}/` 暂存目录；下次构建从 `offline/php/<版本>/` 复制，不能依赖残留暂存文件。

下载进度日志必须输出到 stderr，不能污染命令替换捕获的镜像名、路径或 YAML 内容。

### 2.2 阶段治理与完成定义

- **P0 安全与恢复**：先处理服务状态、配置覆盖、备份恢复、超时和回滚风险。
- **P1 可验证性**：再补测试替身、失败注入、错误码、幂等性和 Compose 校验。
- **P2 结构治理**：最后拆分超长函数、收敛公共模块边界和统一生命周期模板。
- 每个阶段必须有明确的输入、输出、成功条件、失败条件和回滚点。
- 单元只有在代码、测试、文档和迁移说明全部完成后，才能标记为“已完成”。
- 未完成、部分完成、仅有兼容基础和 `SKIPPED` 必须使用不同状态，不得用“基本完成”代替。
- 规划中的目标目录、命令或配置项必须标明“当前状态”和“目标状态”，避免把运行期生成物误认为源码。

### 2.3 可靠性与可重复性基线

- 同一操作重复执行必须幂等；并发执行必须有项目级锁、PID、命令、开始时间和 stale lock 清理策略。
- CLI 错误必须同时提供人类可读日志和稳定错误分类；禁止让调用方依赖中文错误文本判断阶段。
- 版本支持必须记录 PHP/MySQL/Nginx 版本、基础镜像、Alpine 版本、扩展兼容性和已知限制。
- 构建结果应记录镜像 tag、依赖来源、包版本、校验值和配置摘要；离线模式不得意外联网。
- 宿主目录、容器用户、`CURRENT_UID`、`CURRENT_GID` 和 sudo 范围必须有明确权限边界。
- 测试必须区分静态、单元、行为、Compose 和真实 Docker 集成层；外部依赖使用可控替身或明确标记 `SKIPPED`。

### 2.4 状态机和接口契约

资源生命周期至少遵循：

```text
absent -> preparing -> configured -> starting -> healthy -> committed
					   \-> failed -> rolling_back -> rolled_back
```

- 每个状态必须说明检测依据、允许的下一状态、中断恢复方式和孤儿资源清理方式。
- 提交前不得删除旧配置、旧容器、旧镜像、旧数据卷或旧缓存。
- 每个 CLI 命令必须定义参数、默认值、兼容别名、修改资源、幂等行为和稳定错误分类。
- `redis install` 省略版本时使用官方最新稳定主版本 `8`（镜像 tag `redis:8-alpine`）；省略 `--port` 时使用 Redis 默认端口 `6379`；安装完成必须像 MySQL 一样把端口和认证密码持久化到 `.env`，密码键为 `REDIS_<去点版本>_ROOT_PASSWORD`。显式版本和端口必须原样校验并使用。
- Redis 每个版本必须生成并挂载本地可编辑配置 `config/redis/<版本>/redis.conf`；Compose 以只读方式挂载该文件，安装/重建不得用镜像内默认配置替代本地配置。密码继续只保存在 `.env`，不得写入 `redis.conf`。
- stdout 只输出调用方需要捕获的结果；stderr 输出进度、警告、诊断、失败和回滚信息。
- 失败分类至少区分参数、预检、网络、构建、配置、健康检查和回滚失败。
- 回滚失败必须返回独立错误分类，并保留人工恢复所需的快照或诊断信息。

### 2.5 PHP 扩展配置演进

目标结构：

```text
config/php/
├── defaults/extensions.env       # 唯一默认扩展来源
├── 7.4/extensions.env            # 7.4 实际扩展状态
├── 8.0/extensions.env            # 8.0 实际扩展状态
└── 8.4/extensions.env            # 8.4 实际扩展状态
```

- `PHP_DEFAULT_EXTENSIONS` 可以保留为用户覆盖项，但不得在 `install.sh`、运行时脚本和文档中重复维护长列表。
- `php install <版本>` 不带 `--ext` 时读取最终默认配置。
- `--ext` 只覆盖本次安装；`extension add/remove` 只修改对应版本的状态文件。
- 后续可引入 `minimal`、`web`、`debug` profile；在没有明确需求前不扩大改动范围。
- `xdebug` 等重扩展是否默认启用，应评估构建时间、镜像体积和运行开销。

## 3. 目录和架构

```text
phpbox/
├── bin/phpbox                    # CLI 入口和命令路由
├── lib/
│   ├── common.sh                 # 环境、日志、预检、路径、回滚基础设施
│   ├── build.sh                  # PHP Dockerfile、APK/PECL 和镜像构建
│   ├── php.sh                    # PHP 安装、扩展、列表和卸载
│   ├── mysql.sh                  # MySQL 生命周期和端口
│   ├── redis.sh                  # Redis 生命周期和端口
│   ├── nginx.sh                  # Nginx 生命周期、配置和重载
│   ├── site.sh                   # 站点、vhost、PHP 版本切换
│   └── backup.sh                 # 备份、恢复、归档安全检查
├── compose/
│   ├── docker-compose.yml        # 共享网络等主配置
│   └── services/                 # 按服务和版本拆分的 Compose 分片
├── config/                       # 用户配置和运行期生成配置
│   ├── mysql/<版本>/
│   ├── nginx/<版本>/
│   └── php/<版本>/
├── offline/php/<版本>/           # 已验证的 APK/PECL 离线缓存
├── logs/                         # Nginx 和 PHP 日志
├── backups/                      # 备份归档
├── tests/                        # 结构、语法和行为回归测试
├── .github/                      # AI 指令、Skill、Agent、Prompt 和 Hook
├── install.sh                    # 安装和初始化入口
├── AGENTS.md                     # 项目总纲
├── README.md                     # 用户文档
└── .env.example                  # 环境变量模板
```

架构依赖方向固定为：`bin/phpbox` 路由到 `lib/*.sh`，业务模块复用 `common.sh`，构建模块生成 Compose 和服务配置；业务模块不得反向依赖 CLI 入口，也不得复制公共路径、日志、回滚和环境加载逻辑。

## 4. 工程执行流程

每次任务都按以下顺序执行，不跳过本地证据收集或验证：

1. 读取本文件、`.github/copilot-instructions.md`，再读取目标文件匹配的 instructions、skill、agent 或调用方。
2. 执行 `git status --short`，保留用户已有修改，不回滚、不重置、不提交。
3. 找到直接决定行为的入口、函数、生成器或测试。
4. 用一句话提出可证伪的根因假设，并指定最便宜的验证检查。
5. 先说明将修改的文件、阶段、风险和验证方式。
6. 使用最小编辑，配置生成先写临时文件并校验，成功后再替换正式文件。
7. 第一次编辑后立即运行窄验证；失败时只修复当前切片并重跑同一个检查。
8. 完成后检查清理、超时、回滚、终端回显和文档同步。
9. 交付时报告实际修改、验证命令、通过项、失败项和 `SKIPPED` 项。

## 5. 长操作和恢复要求

网络、Docker、Compose、镜像构建、APK/PECL、归档、恢复和健康检查必须：

1. 开始前输出阶段、目标、输入和超时边界。
2. 进行中输出可观察进度；轮询必须有截止时间，禁止无边界等待。
3. 成功时输出结果、验证状态和产物位置。
4. 失败时输出失败阶段、退出状态、旧状态是否保留和下一步。
5. 清理本次新增临时文件、容器、镜像或目录，但不得删除既有可恢复数据。

备份和恢复必须校验归档成员路径，拒绝包含 `..` 的路径；恢复前保留旧配置或具备明确回滚点。

## 6. 代码质量约束

- 优先早返回，控制条件嵌套、函数长度、参数数量和圈复杂度。
- 公共逻辑放入 `common.sh`，避免业务模块复制实现。
- 变量使用有意义的名称，不使用无说明的魔法数字。
- 函数只承担单一职责；下载、校验、晋升、清理和回滚应保持边界清晰。
- 日志必须说明动作和结果；错误必须返回非零退出码。
- 不为与当前任务无关的技术债务做重构。
- Bash 文件保持 ASCII，注释只解释非显然的约束或复杂流程。

### 6.1 复杂度控制

- 单个函数只负责一个阶段：参数解析、准备、执行、验证、提交、清理和回滚应拆开。
- 复杂函数优先按业务阶段拆分，不通过增加更多布尔参数、隐式全局变量或深层嵌套来扩展。
- 新增分支前先判断能否使用早返回、表驱动映射或公共验证函数减少条件嵌套。
- 函数参数应表达稳定契约；超过 5 个参数时优先使用明确的配置变量、局部结构或拆分函数。
- 单个函数超过约 80 行、单个脚本超过约 700 行或嵌套超过 3 层时，必须在规划中说明保留原因或拆分方案。
- 下载、构建、校验、缓存晋升、服务启动和回滚不得继续堆叠到同一个函数中。
- 不以压缩代码行数为目标；可读性、失败路径和可测试性优先于短代码。

### 6.2 可维护性要求

- 公共行为必须有单一实现；日志、路径展开、超时执行、Compose 调用、健康检查和回滚不得在业务模块中复制。
- 全局变量、`trap`、隐式返回码和命令替换的副作用必须有明确边界；函数应说明输入、输出和失败行为。
- 外部命令必须检查退出码；可能阻塞的命令必须有总超时、无响应检测或明确截止时间。
- 用户可感知的操作必须输出阶段、目标、结果和清理状态；错误信息必须指出失败阶段和旧状态是否保留。
- 临时文件、临时容器、临时镜像和构建目录必须在成功与失败路径都可清理。
- 任何迁移、重构或公共函数变更都必须保留既有 CLI 参数语义，或提供兼容路径和迁移说明。
- 代码修改必须伴随最窄测试；无法测试的外部依赖必须使用可控替身或明确标记 `SKIPPED`。

### 6.3 结构合理性和依赖边界

固定依赖方向如下：

```text
bin/phpbox
	-> lib/*.sh
		-> lib/common.sh 的公共能力
		-> compose/ 和 config/ 的生成或校验
		-> Docker/Compose 运行时
```

- `bin/phpbox` 只负责命令路由、参数转发和统一退出结果，不实现业务规则。
- `lib/common.sh` 只提供环境、日志、路径、外部命令执行、公共校验和回滚基础设施；不得持续吸收具体服务的业务逻辑。
- `lib/build.sh` 负责 PHP 构建编排；网络下载、APK/PECL 资产准备、Dockerfile 渲染、镜像构建和缓存晋升应保持可分辨的函数边界。
- `lib/php.sh`、`lib/mysql.sh`、`lib/redis.sh`、`lib/nginx.sh`、`lib/site.sh` 只负责各自领域，不得反向调用 CLI 入口或依赖其他业务模块的私有函数。
- 跨模块调用必须通过公共函数或明确的回调契约；不得依赖文件加载顺序提供隐式函数。
- 生成配置必须先写临时文件并校验，成功后原子替换正式文件；运行期生成物不得被当作源码维护。
- 同一生命周期模式只保留一个公共实现：预检 -> 准备 -> 执行 -> 健康检查 -> 提交/回滚。
- 新增模块前先说明它的职责、依赖方向、状态边界和测试替身，不以复制现有服务脚本作为实现方式。

### 6.4 复杂度和结构验收

每次涉及 Bash 业务逻辑的变更，至少检查：

1. 是否增加了超长函数、深层嵌套、重复分支或新的隐式全局状态。
2. 是否让 `common.sh` 继续承担新的业务职责，或产生业务模块之间的反向依赖。
3. 是否为成功、失败、超时、中断、重复执行和回滚路径提供行为验证。
4. 是否保留临时资源清理、旧配置保护、退出码和终端阶段日志。
5. 是否同步更新相关测试、README、`.env.example` 和 AI 规划文件。

结构重构的完成标准：

- 公共逻辑只有一个实现，业务模块职责可以用一句话说明。
- 失败路径不会比成功路径更隐晦，旧状态和新状态边界明确。
- 函数复杂度没有无说明地上升；必要的复杂度已拆分或记录原因。
- 静态检查、最窄行为测试和受影响的集成检查均有结果；无法执行的项目明确标记 `SKIPPED`。

### 6.5 可读性与 Bash 严格模式

- 用户输入必须统一经过版本、域名、端口和路径校验；删除类命令不得直接拼接未经校验的路径。
- 严格模式下禁止使用 `local name=$(command)` 隐藏命令替换失败；关键命令必须先独立执行并检查退出码。
- 合法空结果必须与命令失败明确区分；`grep`、管道和循环在空输入时不得误触发 `set -e`。
- 全局变量和 `trap` 必须带模块前缀并声明所有权；业务模块不得覆盖公共清理 trap。
- 函数名必须体现职责和副作用；公共函数必须说明输入、输出、修改的持久状态和失败行为。
- 注释解释约束和原因，不重复代码表面行为；复杂流程前保留简短的阶段说明。
- 生成 YAML、Nginx、PHP 和环境配置统一遵循“临时文件 -> 校验 -> 原子替换”。

### 6.6 接口契约与生命周期状态

- 每个 CLI 命令必须明确参数、默认值、兼容别名、stdout/stderr 语义、稳定错误码和幂等行为。
- stdout 只输出调用方需要捕获的结果；进度、警告、诊断和回滚信息输出到 stderr。
- 资源生命周期至少区分 `absent`、`preparing`、`configured`、`starting`、`healthy`、`committed` 和 `rolling_back`。
- 每个状态必须说明持久化依据、允许的下一状态、中断后的恢复方式和孤儿资源清理方式。
- 事务提交前不得删除旧配置、旧容器、旧镜像、旧数据卷或旧缓存；回滚失败必须返回独立错误分类。
- 外部命令调用必须经过可替换的公共边界，便于 fake Docker、网络和 sudo 测试替身注入。

## 7. 配置、安全和兼容性

- `.env` 含 MySQL 密码等本地开发凭据，禁止入仓；本项目允许为便于查阅在本地日志和提示中明文展示密码。
- 新增环境变量必须同步 `.env.example`、加载逻辑、帮助文本和 README。
- 路径支持绝对路径、项目相对路径和 `~/` 展开；路径判断使用显式条件，避免字符串误判。
- 生成文件属于运行期产物，优先修改生成逻辑，不直接把生成物当源码维护。
- 迁移目录时必须提供旧路径兼容、失败回滚和已有安装状态保护。
- 不改变既有 CLI 公共命令和参数语义，除非同时提供兼容别名或明确迁移说明。

### 7.1 生成物和源码归属

- 每个文件必须明确属于源码、模板、运行期生成物、用户持久配置、离线缓存或测试夹具中的一种。
- README、`.gitignore` 和 Git 跟踪状态必须一致；声称“不入仓”的文件不得继续作为生成物被跟踪。
- 生成物不得作为人工维护的事实来源；应修改生成逻辑、模板或用户配置，再重新生成。
- 目标目录、迁移目录和兼容旧路径必须同时记录当前状态、目标状态、迁移触发条件和清理条件。

### 7.2 权限、数据和恢复边界

- 明确宿主目录、容器用户、`CURRENT_UID`、`CURRENT_GID`、Docker root 创建文件和 sudo 的权限边界。
- `--purge` 必须列出精确删除清单；未列出的配置、数据卷、镜像和备份不得删除。
- 备份停止服务失败时不得继续打包；恢复必须先创建快照，所有数据和服务恢复成功后才能提交。
- 磁盘空间不足、权限不足、数据卷缺失和服务启动失败必须进入明确失败路径，并清理本次新增资源。

## 8. 测试和验收

优先执行与修改切片最接近的检查；完整验收顺序如下：

```bash
bash -n install.sh bin/phpbox lib/*.sh
bash tests/lint.sh
bash tests/run.sh
git diff --check
```

涉及 Compose 时额外执行 YAML/Compose 配置校验；涉及 Docker、网络或真实服务时执行带超时的集成检查。若测试文件或依赖不存在，必须报告 `SKIPPED`，不得声称通过。

验收至少覆盖：

- 安装、卸载、端口修改和重复执行的幂等性。
- Docker/APK/PECL 失败不会污染正式离线缓存。
- Compose 生成文件可解析，日志不会污染命令替换结果。
- 站点配置失败会回滚，Nginx 配置通过校验后才重载。
- 备份包含必要配置和离线缓存，恢复拒绝危险归档路径。
- 每个长操作有阶段日志、超时边界和清理结果。
- 非法域名、版本、端口和路径会在修改状态前失败。
- 关键 Docker、Compose、网络、构建和归档命令均通过统一超时边界执行。
- PHP 镜像完成扩展验证后才允许晋升 APK/PECL 缓存。
- README、帮助文本、`.env.example`、`.gitignore` 和实际 Git 跟踪状态保持一致。

## 9. 文档与 AI 配置

- 用户命令、环境变量、目录结构、生成物和备份恢复流程维护在 `README.md`。
- 项目级常驻规则维护在 `.github/copilot-instructions.md`。
- 文件类型规则维护在 `.github/instructions/`。
- 多步骤维护流程维护在 `.github/skills/phpbox-maintenance/SKILL.md`。
- 审查角色维护在 `.github/agents/`，一次性流程维护在 `.github/prompts/`。
- `Subagent.md` 只做人工索引，不作为自动加载入口。
- 新增或修改 AI 配置后检查 frontmatter、JSON 和路径引用。
- 所有的步骤在终端都必须有回显，便于用户知晓当前脚本执行到哪一步的什么状态，对于容易超时的操作要加超时限制。