# phpbox 全盘重构：最终实施计划（四阶段，每阶段独立 git commit）

## 阶段一：清理与卫生（零行为变更）
1. 删除 lib/php.sh 中重复的 `_php_resolve_build_proxy` 旧定义（273-288 行死代码，保留 295 行生效版；唯一调用方为无参调用、零行为影响），旧版的背景教训注释并入存活版
2. lib/php.sh `_php_build_image`：`apk_mirror` 改为主源推导 `${APK_MIRRORS%% *}`，删除过时的阿里云回退条件（默认列表首位已含 aliyun）
3. 【新】lib/php.sh `_php_docker_build_verified` 成功路径末尾追加：构建成功晋升后清空暂存目录（config/php/<版本>/{pecl,apk}，正本在备份库）——消除 phpbox backup 的双份冗余与磁盘膨胀
4. 修正 php.sh 263 行过时注释（apk-cache、pecl-cache → offline/{apk,pecl}）；合并 detect/resolve 两函数重复的背景注释
5. install.sh：shebang 归位第 1 行（现被中文注释顶到第 2 行）、移除已删除的 data/ 目录创建、生成的默认 .env 补新增键（PHP_DEFAULT_EXTENSIONS/APK_MIRRORS/OFFLINE_DIR/BUILD_PROXY/NGINX_VERSION）
6. tests/run.sh：source 之后重设套件清理 trap（common.sh 的 source-time trap 会覆盖它，导致每次测试泄漏 /tmp 临时环境）
7. .env.example 补 APK_MIRRORS 说明（多源空格分隔、按序故障转移、值截取到 /alpine 含）
8. 安装 shellcheck 并在 tests/lint.sh 增加第四道静态检查门禁（全项目 0 告警为目标）
9. 用户侧：清理 /tmp 泄漏的测试环境（rm -rf /tmp/tmp.*/phpbox）
验收：bash tests/run.sh 70 断言全绿 + lint 通过且 shellcheck 零告警（零行为变更的证明）+ 构建成功后暂存目录为空

## 阶段二：php.sh 拆分（纯函数搬移，行为不变，~1 小时）
1. 新建 lib/build.sh（约 350 行）：迁入离线构建管线全部函数——`_php_stage_pecl_tarballs`、`_php_promote_pecl_tarballs`、`_php_stage_apk_closure`、`_php_render_dockerfile`、`_php_build_image`、`_php_docker_build_verified`、`_php_discard_staging`、`_php_resolve_build_proxy`、`_php_detect_build_proxy`、`_php_online_build_args`、`_php_split_ext_list`、`_php_ext_apk_deps`、`_php_pecl_tarball_url`、`_php_mirror_host_args`、`_PHP_PECL_REMOTE_EXTS`、`_PHP_EXT_APK_DEPS`、`_PHP_BUILD_BASE_APK_DEPS`、`_PECL_STAGED`
2. lib/php.sh 瘦身（约 300 行）：配方读写/校验/推断 + install/uninstall/extension/list + `_php_cleanup_images`
3. bin/phpbox 与 tests/run.sh 的 source 列表插入 build.sh（置于 common 之后、php 之前）
4. 验收：tests/run.sh ≥70 断言全绿、lint 通过（php.sh/build.sh 均满足 60 行/4 层门禁）、跨界调用 `_php_generate_compose → _php_build_image` 正常

## 阶段三：gd 依赖补齐 + 闭包重取（打通离线构建的最后一环）
1. `_PHP_EXT_APK_DEPS` 的 gd 条目补 `zlib-dev`（IPE 在线构建证实必需，当前映射遗漏；libwebp-dev 已在）
2. 删除过期闭包 `offline/php/8.0/apk/` → `phpbox php install 8.0` 重新预取（新闭包含 zlib-dev/libwebp-dev 全套）
3. 验收：离线构建全程通过 gd 编译 → `[OK] PHP 8.0 安装完成`

## 阶段四：lib/offline.sh 镜像归档模块（按已确认的三项决策）
1. 新模块（约 150 行）：`_offline_archive_path <服务> <版本>`（offline/images/<服务>-<版本>.tar，nginx 版本取 NGINX_VERSION）、`_php_offline_image_list <服务> <版本>`（case 表：php=php基础镜像+IPE+成品；mysql=mysql:${ver}；redis=redis:${ver}-alpine；nginx=nginx:${NGINX_VERSION}）、`_offline_ensure_image <镜像> <归档>`（inspect→有则 0；归档在→docker load 复验；皆无→1）
2. 命令：`cmd_offline_status`（遍历 compose/services 已装服务，逐镜像 ✓/✗ + pecl/apk 备份计数 + 汇总"离线就绪/缺失 N 项"）、`cmd_offline_export <服务> [版本]`（版本缺省=全部已装，docker save 多镜像 -o 归档）、`cmd_offline_import <tar>`（docker load）、`cmd_offline_pull <服务> [版本]`（在线预拉，成品镜像跳过并打印说明）
3. 四处 install 接入：compose up 前调 `_offline_ensure_image`（mysql/redis/nginx/php 各自的镜像与归档；皆无→静默走在线 pull）
4. bin/phpbox：`offline)` 分发（require_docker 前置）+ help 增加命令段
5. backup.sh：备份收录范围收窄为构建资产（offline/php/），显式排除 offline/images/（GB 级归档不入备份，迁移=复制 offline/images/ 本身）
6. tests/run.sh 第 9 节：桩 docker 测归档命名/export 镜像清单/ensure 三态/status 输出
7. README 补 offline 架构章节（备份库/暂存/镜像归档/多镜像源）
验收：全量测试 ≥74 断言全绿；`phpbox offline status` 输出全服务矩阵

## 阶段五（新②）：common.sh 服务专属代码归位（与阶段二同期或紧随）
1. `_init_nginx_config`、`_nginx_inject_sites_include`、`SITES_MOUNT_PATH/SITES_INCLUDE_LINE` 常量迁至 lib/nginx.sh；`_init_mysql_config` 迁至 lib/mysql.sh；`_init_php_config` 迁至 lib/php.sh
2. common.sh 的 `init_config_files` 保留为分发器（调用各模块实现，消除通用模块对服务模块的反向依赖）
3. 验收：tests/run.sh 相关断言全绿、lint 通过

## 全局验收标准
1. bash tests/run.sh ≥74 断言全绿；bash tests/lint.sh 通过且 shellcheck 零告警
2. 断网演练：offline import 后 php/mysql/redis install 全部成功
3. php.sh ≈300 行单一职责（生命周期）、build.sh ≈350 行单一职责（构建管线）
4. 每阶段一个 git commit，可独立回退

## 明确不做（防过度设计）
不引入 JSON/YAML 解析（APK_MIRRORS 空格分隔即 shell 惯例"数组"）；不做通用包管理器抽象（mysql/redis/nginx 无构建步骤）；不上 bash 框架；镜像归档不进 phpbox backup（已确认）。