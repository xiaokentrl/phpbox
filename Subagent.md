# Subagent 说明

本文件是给维护者阅读的索引，不是 VS Code/Copilot 的标准自动加载文件。

## 独立单元实施总表

各单元按依赖顺序实施。每个单元完成后先通过本单元验证，再进入下一个单元；不要在一个单元内混入无关重构。

| 单元 | 目标 | 前置条件 | 主要文件 | 当前状态 |
| --- | --- | --- | --- | --- |
| U0 基线与测试 | 建立可重复的语法、结构和行为基线 | 无 | `tests/`、`lib/*.sh`、`bin/phpbox` | 待确认 |
| U1 配置单一来源 | 消除 PHP 默认扩展列表重复维护 | U0 | `config/php/defaults/`、`.env.example`、`install.sh`、`lib/common.sh`、`lib/php.sh` | 规划中 |
| U2 PHP 状态归位 | 将顶层 `state/` 迁移到版本配置目录 | U0、U1 | `lib/common.sh`、`lib/php.sh`、`lib/backup.sh`、`.gitignore` | 已有兼容基础，待完成 |
| U3 构建事务化 | APK/PECL 构建成功后才晋升正式离线缓存 | U0 | `lib/build.sh`、`lib/common.sh`、测试文件 | 主流程已实现，待回归 |
| U4 服务生命周期 | 统一 PHP/MySQL/Redis/Nginx 的安装、启动、停止和健康检查 | U0、U3 | `lib/*.sh`、`compose/`、`bin/phpbox` | 待实施 |
| U5 站点与端口回滚 | 保证站点、hosts 和端口变更失败不破坏旧状态 | U0、U4 | `lib/site.sh`、`lib/nginx.sh`、`lib/mysql.sh`、`lib/redis.sh` | 待实施 |
| U6 备份与恢复 | 备份完整、恢复安全、失败可回滚 | U0、U2、U4 | `lib/backup.sh`、`README.md`、测试文件 | 部分实现，待补验 |
| U7 文档与 AI 配置 | 让命令、目录、规则和工作流保持同步 | U1-U6 | `README.md`、`AGENTS.md`、`.github/` | 基础配置已完成 |
| U8 并发控制与错误契约 | 防止并发事务互相覆盖，提供稳定失败分类 | U0 | `lib/common.sh`、`bin/phpbox`、测试文件 | 待实施 |
| U9 版本支持与可重复构建 | 明确版本矩阵、依赖来源和离线可复现边界 | U0、U3 | `README.md`、`.env.example`、`lib/build.sh`、配置模板 | 待实施 |
| U10 权限与数据安全 | 明确宿主目录、容器用户和数据卷生命周期 | U0、U4、U6 | `lib/common.sh`、`lib/backup.sh`、`lib/*.sh` | 待实施 |
| U11 测试分层与替身 | 建立不依赖 Docker 的稳定测试和失败注入 | U0 | `tests/`、测试替身目录、`lib/*.sh` | 待实施 |
| U12 结构重构与生命周期收敛 | 拆分超长函数，消除公共模块反向依赖和重复模板 | U3-U5、U11 | `lib/common.sh`、`lib/build.sh`、`lib/backup.sh`、业务模块 | 待实施 |

每个单元的交付必须记录：实际修改文件、执行命令、通过项、`SKIPPED` 项、残余风险和回滚结果。

## 单元规划统一模板

新增或调整单元时，必须按以下字段描述，不能只写目标和口号：

```text
目标：要解决的用户或系统问题
输入：命令、配置、现有资源和前置状态
输出：文件、配置、容器、缓存或可观察结果
修改文件：允许修改的源码、测试和文档
不修改文件：明确保护的用户配置、数据和既有资源
依赖：前置单元、外部命令和运行环境
实施步骤：按准备、执行、验证、提交、清理排列
失败注入点：可控的失败位置和预期结果
验证命令：最窄测试、静态检查和必要的集成检查
退出条件：满足后才允许进入下一单元
完成状态：未开始/规划中/部分实现/实现完成待验证/验证完成/阻塞/SKIPPED
回滚方式：旧状态、快照、临时资源和人工恢复路径
```

## 阶段退出条件

单元不能因为“代码已写完”直接进入下一单元，必须满足：

1. 正常路径和至少一个关键失败路径均已验证。
2. 旧配置、旧数据、旧容器和旧缓存的保护边界已验证。
3. 临时文件、临时容器、锁和测试替身状态已清理。
4. stdout/stderr、错误分类、退出码和阶段日志符合接口契约。
5. README、`.env.example`、`.gitignore` 和迁移说明已同步。
6. 不可执行的外部检查明确标为 `SKIPPED`，并记录原因和残余风险。

## 失败注入矩阵

高风险单元至少覆盖以下故障点：

| 阶段 | 失败注入 | 预期结果 |
| --- | --- | --- |
| 配置写入 | 临时文件写入或原子替换失败 | 保留旧配置，清理临时文件 |
| 输入校验 | 非法域名、版本、端口或路径 | 修改状态前失败 |
| Docker 构建 | 构建返回非零或超时 | 不晋升缓存，清理新资源 |
| 健康检查 | 服务超时或检查命令失败 | 标记失败，恢复旧服务 |
| 端口修改 | 新端口被占用 | 恢复旧端口和旧容器 |
| 备份 | 停服、打包或磁盘空间失败 | 不提交不完整归档 |
| 恢复 | 配置或数据卷恢复失败 | 使用恢复前快照回滚 |
| 并发锁 | 活跃锁或 stale lock | 不覆盖活跃事务，按规则清理 stale lock |

## 迁移检查模板

所有路径、格式或配置迁移都必须记录：

```text
旧格式/旧路径：
新格式/新路径：
触发条件：
迁移前备份点：
校验条件：
失败处理：
清理条件：
兼容截止条件：
```

## 可读性检查模板

每个代码单元完成前检查：

- 函数名表达职责和副作用。
- 变量名表达业务含义，不使用无说明的一字母变量。
- 没有重复常量、隐式全局状态或未声明的加载顺序依赖。
- 没有 `local value=$(command)` 形式的隐藏失败。
- 空结果和命令失败有明确区分。
- 注释解释约束和原因，不重复代码表面行为。
- 日志包含阶段、目标、动作和结果。
- 复杂度超过阈值时已拆分，或记录了保留原因。

## U0：基线与测试

**目标**：先确认当前行为和测试缺口，防止后续结构调整把已有功能弄坏。

**实施步骤**：

1. 执行 `git status --short`，记录用户已有修改。
2. 检查 `tests/lint.sh` 和 `tests/run.sh` 是否存在；不存在时标记 `SKIPPED`，不能假称通过。
3. 执行 `bash -n install.sh bin/phpbox lib/*.sh`。
4. 执行最窄的相关测试，再执行 `bash tests/lint.sh` 和 `bash tests/run.sh`。
5. 记录当前 CLI、生成配置、失败清理和日志行为。

**验收标准**：语法检查通过；现有测试结果可复现；缺失测试、依赖和 Docker daemon 状态有明确记录。

**回滚方式**：本单元只读，不产生业务文件修改；测试临时目录必须在结束时清理。

## U1：配置单一来源

**目标**：默认扩展列表只维护一份，用户覆盖和版本实际状态职责分离。

**实施文件**：`config/php/defaults/extensions.env`、`.env.example`、`install.sh`、`lib/common.sh`、`lib/php.sh`、`README.md`。

**实施步骤**：

1. 新增 `config/php/defaults/extensions.env`，保存唯一默认 `PHP_DEFAULT_EXTENSIONS`。
2. 修改 `load_env`：先读取默认配置，再读取 `.env`，让用户配置覆盖默认值。
3. 删除 `install.sh` 和运行时脚本中的重复长列表，但保留兼容的环境变量名称。
4. 保证 `php install <版本> --ext ...` 只覆盖当前安装，不修改默认文件。
5. 更新帮助文本、`.env.example` 和 README 的配置来源说明。
6. 为默认值、用户覆盖和 `--ext` 覆盖各增加一个回归检查。

**验收标准**：改变默认文件即可影响无 `--ext` 安装；`.env` 覆盖有效；单次 `--ext` 不污染默认配置；旧命令参数仍可用。

**回滚方式**：先备份旧默认列表和 `.env`；新配置加载失败时保留旧读取路径，不能覆盖用户 `.env`。

## U2：PHP 状态归位

**目标**：将 `state/php-*-extensions.env` 迁移为 `config/php/<版本>/extensions.env`，消除顶层泛化状态目录。

**实施文件**：`lib/common.sh`、`lib/php.sh`、`lib/backup.sh`、`.gitignore`、`README.md`。

**实施步骤**：

1. 定义新路径 `config/php/<版本>/extensions.env`，所有新安装和扩展变更只写新路径。
2. 启动时检测旧路径 `state/php-*-extensions.env`，仅在新路径不存在时迁移。
3. 迁移前创建临时副本并校验变量格式；写入成功后再删除旧文件。
4. 若任意写入或校验失败，保留旧文件并继续使用旧路径，报告迁移失败原因。
5. 备份逻辑改为随 `config/php/` 自动包含新状态，同时兼容旧备份中的 `state/`。
6. 更新 `.gitignore`、目录文档和恢复逻辑。

**验收标准**：新旧安装都能读取扩展状态；重复执行迁移幂等；迁移失败不丢配置；备份和恢复均覆盖状态文件。

**回滚方式**：迁移事务保留旧文件；新路径损坏时从旧文件恢复，不执行无确认的目录删除。

## U3：构建事务化

**目标**：APK 和 PECL 依赖只有在镜像构建与扩展验证全部成功后，才进入 `offline/` 正式缓存。

**实施文件**：`lib/build.sh`、`lib/common.sh`、`lib/php.sh`、`tests/run.sh`。

**实施步骤**：

1. 为每次 PHP 版本构建创建独立暂存目录。
2. 从 `offline/php/<版本>/pecl/` 和 `offline/php/<版本>/apk/` 命中时复制到暂存区；未命中时下载到暂存区。
3. 校验 APK 闭包、关键构建依赖和 PECL 包完整性。
4. 生成 Dockerfile，执行 Docker build，并验证 `php -m`、`php --ri` 和 `php-fpm -t`。
5. 所有验证成功后，使用临时目录和原子替换晋升 APK/PECL 缓存。
6. 生成 Compose、启动容器并执行健康检查；失败时只清理本次产物。
7. 所有进度日志写入 stderr，避免污染命令替换捕获的镜像名或 YAML。

**验收标准**：构建失败不新增正式缓存；成功后缓存可复用；Compose 可解析；长操作持续输出阶段、进度、结果和清理状态。

**回滚方式**：保留旧离线缓存和旧镜像；晋升失败时删除本次临时目录，不删除旧版本缓存。

## U4：服务生命周期

**目标**：让 PHP、MySQL、Redis、Nginx 的安装、启动、停止、卸载和健康检查遵循一致生命周期。

**实施文件**：`lib/php.sh`、`lib/mysql.sh`、`lib/redis.sh`、`lib/nginx.sh`、`lib/common.sh`、`compose/`、`bin/phpbox`。

**实施步骤**：

1. 先在 `common.sh` 统一 Docker/Compose 预检、项目名、网络名、超时和日志函数。
2. 每个服务实现“预检 -> 生成配置 -> 校验 -> 启动 -> 健康检查 -> 成功提交”的顺序。
3. 安装前检测端口和已有容器，避免覆盖不同版本的运行实例。
4. 卸载默认保留配置和数据；只有 `--purge` 才删除明确声明的资源。
5. 所有 Docker、Compose 和健康检查命令增加超时或无响应截止时间。
6. 为重复安装、重复启动、服务不存在和服务启动失败增加回归测试。

**验收标准**：重复执行幂等；服务失败时旧服务和旧配置可恢复；端口冲突有明确提示；健康检查失败返回非零状态。

**回滚方式**：生成配置使用临时文件；启动失败时恢复旧 Compose 和服务状态，不清除已有数据卷。

## U5：站点与端口回滚

**目标**：站点、hosts、Nginx 配置和 MySQL/Redis 端口变更具备事务性。

**实施文件**：`lib/site.sh`、`lib/nginx.sh`、`lib/mysql.sh`、`lib/redis.sh`、`lib/common.sh`、测试文件。

**实施步骤**：

1. 读取并保存变更前的配置、端口和 hosts 状态。
2. 先写临时 vhost 或 Compose 文件，使用 Nginx/Compose 校验命令验证。
3. 校验通过后原子替换正式文件，再执行 reload 或重建服务。
4. reload、重建或 hosts 更新失败时恢复旧文件、旧端口和旧解析。
5. `site switch` 只在目标 PHP 服务健康后切换 vhost。
6. 为域名非法、端口占用、配置校验失败和 reload 失败增加回归测试。

**验收标准**：失败变更不留下半份配置；成功变更可访问；重复执行不产生重复 hosts 或重复 vhost。

**回滚方式**：每次变更使用带版本或时间标记的临时备份；回滚失败时保留备份并明确提示人工恢复路径。

## U6：备份与恢复

**目标**：备份足以迁移本地开发环境，恢复拒绝危险归档并保留恢复前状态。

**实施文件**：`lib/backup.sh`、`README.md`、`.gitignore`、测试文件。

**实施步骤**：

1. 明确备份清单：`.env`、`config/`、`compose/`、`offline/`、站点配置和必要数据卷。
2. 备份前输出阶段并停止相关服务；停止失败时不继续打包。
3. 创建归档临时文件，完成后原子移动到 `backups/`。
4. 恢复前枚举归档成员，拒绝绝对路径和包含 `..` 的路径。
5. 恢复前备份当前配置；解包、校验和服务启动全部成功后再删除临时备份。
6. 恢复失败时恢复旧配置并报告服务是否已重新启动。

**验收标准**：归档可列出且内容完整；危险路径被拒绝；恢复失败不覆盖旧配置；密码和本地开发凭据按项目规则可查阅。

**回滚方式**：恢复过程始终保留恢复前快照；任何校验或启动失败都从快照回滚。

## U7：文档与 AI 配置

**目标**：源码行为、用户文档和 Copilot 工作流描述一致。

**实施文件**：`README.md`、`AGENTS.md`、`Subagent.md`、`.github/`、`.env.example`。

**实施步骤**：

1. 新增或修改环境变量时同步 `.env.example`、加载逻辑、帮助文本和 README。
2. 生成文件、运行期目录、缓存和备份内容变化时同步 README 目录树。
3. 维护任务的多步骤流程写入 Skill；只读审查写入 Agent；单次检查写入 Prompt。
4. 保持 `Subagent.md` 为人工索引，不依赖它作为自动加载入口。
5. 校验 Markdown 章节、Agent/Skill frontmatter、Hook JSON 和路径引用。
6. 最后执行 `git diff --check`，并报告文档与实现之间尚未同步的项目。

**验收标准**：用户能从 README 找到命令和配置来源；AI 配置职责不重复；所有引用路径存在；未完成规划明确标注状态。

**回滚方式**：文档或 AI 配置校验失败时只回退本次文档变更，不改动业务脚本和运行期数据。

## U8：并发控制与错误契约

**目标**：保证安装、构建、端口、站点、备份和恢复事务不会并发互相覆盖，并让调用方可靠识别失败阶段。

**实施步骤**：

1. 在 `common.sh` 增加项目级锁，记录 PID、命令、开始时间和工作目录。
2. 区分全局锁、PHP 版本锁和服务锁，避免无关版本互相阻塞。
3. 处理 stale lock：检查 PID 是否仍存在，清理前输出原因，禁止无提示覆盖活跃事务。
4. 定义稳定错误分类，例如参数、预检、网络、构建、配置、健康检查和回滚失败。
5. 保持现有 CLI 参数和人类可读日志不变；稳定错误码只作为额外契约。
6. 测试同时安装、锁释放、异常退出、重复执行和各阶段错误码。

**验收标准**：并发操作不会覆盖配置、端口或缓存；异常退出不会永久阻塞后续命令；错误码能区分失败阶段。

**回滚方式**：锁创建失败不修改业务状态；事务结束或失败都释放锁，锁清理失败时保留诊断文件并提示人工处理。

## U9：版本支持与可重复构建

**目标**：明确支持矩阵，并让同一版本在相同输入下尽可能产生相同构建结果。

**实施步骤**：

1. 建立 PHP、MySQL、Nginx 版本支持矩阵，记录基础镜像和 Alpine 版本。
2. 记录每个扩展的内置/PECL 类型、兼容版本和已知限制。
3. 为 APK/PECL 记录来源、版本、文件名和校验值；构建日志输出配置摘要。
4. 固定或明确声明镜像 tag、镜像源和缓存刷新策略。
5. 增加离线模式：缓存不完整时明确失败，不隐式联网。
6. 测试同一输入重复构建、缓存命中、缓存缺失和镜像源切换。

**验收标准**：用户能查到版本是否支持、依赖从何处来、何时允许联网；构建失败不会产生无法识别的半成品缓存。

**回滚方式**：版本矩阵或缓存元数据不兼容时保留旧缓存和旧镜像，不自动升级或删除已有依赖。

## U10：权限与数据安全

**目标**：明确宿主机目录、容器用户、数据卷、sudo 和 `--purge` 的边界。

**实施步骤**：

1. 梳理 `WWW_ROOT`、`MYSQL_DATA_ROOT`、日志、配置和备份目录的创建属主与权限。
2. 验证 `CURRENT_UID/CURRENT_GID` 与容器内 `www-data` 的映射，覆盖 Docker root 创建文件的情况。
3. 限定 sudo 只用于 hosts 和必要的系统软链，不让业务脚本扩大权限范围。
4. 明确 MySQL、Redis 数据卷是否备份、恢复前是否停服以及现有数据是否允许覆盖。
5. 明确 `--purge` 的删除清单、确认方式和失败后的保留数据。
6. 测试权限不足、磁盘空间不足、数据卷不存在和恢复中断。

**验收标准**：新建文件可被宿主用户使用；权限修复失败返回非零；`--purge` 不删除未声明路径；数据恢复前存在快照。

**回滚方式**：权限变更失败恢复旧属主和模式；数据操作失败保留原卷和恢复前快照。

## U11：测试分层与替身

**目标**：在没有 Docker daemon 或网络的环境中，仍能验证核心逻辑和失败路径。

**实施步骤**：

1. 建立静态层：Bash 语法、ShellCheck（可用时）、Markdown、JSON 和 Compose 解析。
2. 建立单元层：路径展开、配置覆盖、扩展解析、端口计算、错误码和归档路径校验。
3. 建立行为层：安装、迁移、回滚、重复执行、并发锁和失败注入。
4. 增加 fake `docker`、fake `docker compose`、fake `curl`、fake `sudo`，记录调用参数和可控退出码。
5. 将真实 Docker、网络和镜像构建标记为集成层，并为每项设置超时。
6. 测试报告统一使用 `PASS`、`FAIL`、`SKIPPED`，附命令和跳过原因。

**验收标准**：核心失败路径不依赖真实 Docker；测试可重复运行；外部依赖不可用时不会误报通过。

**回滚方式**：测试替身只位于测试目录，不改变生产脚本的 PATH 或用户环境；替身失败不影响现有运行期数据。

## U12：结构重构与生命周期收敛

**目标**：在行为基线稳定后降低代码复杂度和模块耦合，不改变 CLI 公共语义。

**实施步骤**：

1. 先拆分 `cmd_backup`、`cmd_restore`、PHP 构建和通用安装/端口函数，按准备、执行、验证、提交、清理分段。
2. 将 `common.sh` 限定为环境、日志、路径、外部命令、公共校验和回滚基础设施。
3. 消除 `common.sh` 对业务模块私有函数的反向调用，改用公共函数或显式回调契约。
4. 统一 PHP、MySQL、Redis、Nginx 的健康检查和 Compose 执行模板。
5. 每次只重构一个行为切片，先跑原有测试，再跑结构和失败路径测试。
6. 对超过复杂度阈值的保留函数记录原因，不为了形式拆出难以理解的小函数。

**验收标准**：公共逻辑只有一个实现；模块可独立加载或通过明确入口测试；成功、失败、超时和回滚行为与重构前一致。

**回滚方式**：每个重构切片独立提交文件差异但不创建 Git commit；验证失败时只恢复当前切片，不回退用户其他修改。

## 执行顺序

推荐实施顺序：`U0 -> U11 -> U8 -> U1 -> U2 -> U3 -> U4 -> U5 -> U10 -> U9 -> U6 -> U12 -> U7`。

AI 处理任务时按以下顺序使用这些配置：

1. 先读根目录 `AGENTS.md` 和 `.github/copilot-instructions.md`。
2. 按目标文件匹配 `.github/instructions/*.instructions.md`。
3. 维护任务按需加载 `.github/skills/phpbox-maintenance/SKILL.md`。
4. 代码审查使用 `.github/agents/phpbox-reviewer.agent.md` 或其对应 Prompt。
5. 发布前检查使用 `.github/prompts/release-check.prompt.md`。
6. 每次编辑后执行最窄验证；完成时报告实际命令、结果、跳过项和残余风险。

项目实际使用的自定义 Agent 位于：

- `.github/agents/phpbox-reviewer.agent.md`

项目级常驻规则位于：

- `AGENTS.md`
- `.github/copilot-instructions.md`

专项工作流位于：

- `.github/skills/phpbox-maintenance/SKILL.md`

可复用 Prompt 位于：

- `.github/prompts/project-review.prompt.md`
- `.github/prompts/release-check.prompt.md`

如果新增子代理，应使用 `.github/agents/*.agent.md`，不要新增其他命名形式的 `Subagent.md` 作为自动配置入口。

## 配置职责

- `AGENTS.md`：项目规划模板和通用交付流程。
- `copilot-instructions.md`：所有 Copilot 任务的常驻规则。
- `instructions`：按文件类型应用的技术规则。
- `SKILL.md`：多步骤维护工作流和验证清单。
- `*.agent.md`：只读代码审查角色。
- `*.prompt.md`：一次性的审查或发布检查任务。
- `hooks/*.json`：会话生命周期的确定性提示或检查。
